---
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';

interface Props {
	title: string;
}

const { title } = Astro.props;

const userToken = Astro.cookies.get("token")
console.log("userToken: ", userToken?.value)
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title} - SKRTEEEEEE Clean Arc</title>
	</head>
	<body class="min-w-full min-h-screen flex flex-col items-center gap-4 py-2 bg-background text-foreground">
		<Header userToken={userToken} />
		<div class="w-full px-8">
		  <slot />
		</div>
		<Footer />
	</body>
</html>
<style is:global>

	html {
		font-family: system-ui, sans-serif;
	}
	
</style>
